version: "3.8"

services:
    db:
        image: postgres:15-alpine
        container_name: bill_digitizer_db
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: password
            POSTGRES_DB: bill_digitizer_db
        volumes:
            - postgres_data:/var/lib/postgresql/data
        ports:
            - "5432:5432"
        restart: unless-stopped
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U postgres -d bill_digitizer_db"]
            interval: 10s
            timeout: 5s
            retries: 5

    pgadmin:
        image: dpage/pgadmin4:latest
        container_name: bill_digitizer_pgadmin
        environment:
            PGADMIN_DEFAULT_EMAIL: admin@example.com
            PGADMIN_DEFAULT_PASSWORD: admin
            PGADMIN_CONFIG_SERVER_MODE: "False"
        volumes:
            - pgadmin_data:/var/lib/pgadmin
        ports:
            - "5050:80"
        depends_on:
            - db
        restart: unless-stopped

    backend:
        build:
            context: ./backend
            dockerfile: Dockerfile
        container_name: bill_digitizer_backend
        command: sh -c "/app/prestart.sh && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
        volumes:
            - ./backend:/app # Mounts the backend code for live reload
        ports:
            - "8000:8000"
        env_file:
            - ./backend/.env # Loads environment variables from backend/.env
        environment:
            # Explicitly set database connection variables for Docker environment
            POSTGRES_SERVER: db
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: password
            POSTGRES_DB: bill_digitizer_db
            POSTGRES_PORT: 5432
        depends_on:
            db:
                condition: service_healthy # Waits for db to be healthy
        restart: unless-stopped

    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile
        container_name: bill_digitizer_frontend
        ports:
            - "3000:80"
        depends_on:
            - backend
        restart: unless-stopped

volumes:
    postgres_data:
        driver: local
    pgadmin_data:
        driver: local
